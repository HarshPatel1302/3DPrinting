# Workspace layout (parent folder)

If you cloned only this repo, you may also keep a sibling folder for Remotion videos:

| Location | Purpose |
|----------|---------|
| This repo (`final-3d-printing-website` when cloned alone, or project root) | Next.js site — **The Print Patel** |
| `../remotion-print-patel/` (optional sibling) | Remotion reel project (`PrintPatelPromo`) |

Cursor MCP config usually lives in `.cursor/` at the workspace root (editor-local; do not commit secrets).
